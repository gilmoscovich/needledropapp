// hooks/useBookmarks.ts
// Re-exports useBookmarks from BookmarksContext.
// All state is shared app-wide — saves are immediately visible everywhere.
export { useBookmarks } from '@/contexts/BookmarksContext';
